version: 1
routines:
  - name: agent-feedback
    on:
      schedule:
        - cron: '0 9 * * 1'
    timezone: Australia/Melbourne
    mode: propose
    enabled: true
  - name: ci-review
    on:
      schedule:
        - cron: '0 10 * * 1'
    timezone: Australia/Melbourne
    mode: propose
    enabled: true
  - name: dependency-updates
    on:
      schedule:
        - cron: '0 8 * * 1'
    timezone: Australia/Melbourne
    mode: propose
    enabled: true
